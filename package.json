{
  "name": "the-energy-hour",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "generate:seo": "node scripts/generate-seo-content.js",
    "dev": "vite",
    "build": "npm run generate:seo && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
